pyyaml>=6.0
pytest>=7.0
tqdm>=4.66

# Optional for real MedMCQA loading, evaluation, and QLoRA training.
datasets>=2.18
torch>=2.1
transformers>=4.43
accelerate>=0.31
peft>=0.11
trl>=0.9
bitsandbytes>=0.43; platform_system != "Windows"

